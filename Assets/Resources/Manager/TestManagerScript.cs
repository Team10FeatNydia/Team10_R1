using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// this script can be deleted, cause SOURCTREE doesn't accept 0 files inside a folder, so it will be automatically deleted.
public class TestManagerScript : MonoBehaviour 
{
	public enum Meneger
	{
		GER,
		MEMER,
		GEGER,
		CAT
	};

	// Use this for initialization
	void Start () 
	{
		
	}
	
	// Update is called once per frame
	void Update () 
	{
		
	}
}
